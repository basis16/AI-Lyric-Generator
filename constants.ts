import { Genre, Emotion, SongStructure, SoundEngine, ImageStyle, Language } from './types';

export const GENRE_OPTIONS = Object.values(Genre);
export const EMOTION_OPTIONS = Object.values(Emotion);
export const STRUCTURE_OPTIONS = Object.values(SongStructure);
export const SOUND_ENGINE_OPTIONS = Object.values(SoundEngine);
export const IMAGE_STYLE_OPTIONS = Object.values(ImageStyle);
export const LANGUAGE_OPTIONS = Object.values(Language);
